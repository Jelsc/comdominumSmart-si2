# URL de la API del backend
# Si no se especifica, se construye dinámicamente usando window.location + puerto 8000
VITE_API_URL=http://127.0.0.1:8000

# ID de cliente de Google OAuth (opcional)
VITE_GOOGLE_CLIENT_ID=
